name: Build Gradle project
on: workflow_dispatch
jobs:
  print:
    runs-on: ubuntu-latest
    steps:
      - name: Print greetings to console
        run: echo Hello github Actions
      - name: Print a few lines
        run: |
          echo First line
          echo Second line
